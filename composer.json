{
  "name": "onelab/one",
  "description": "Just one swoole framework.",
  "type": "library",
  "homepage": "https://gitlab.com/onelab/one.swoole",
  "authors": [
    {
      "name": "Allen Luo",
      "email": "movoin@gmail.com",
      "homepage": "http://movoin.com"
    }
  ],
  "autoload": {
    "psr-4": {
      "One\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "One\\Tests\\": "tests/"
    }
  },
  "config": {
    "preferred-install": "dist",
    "secure-http": false
  },
  "minimum-stability": "stable",
  "prefer-stable": true,
  "require": {
    "php": "^7.0",
    "ext-swoole": ">=2.1.0",
    "psr/container": "^1.0",
    "psr/http-message": "^1.0",
    "psr/log": "^1.0",
    "monolog/monolog": "^1.23",
    "symfony/yaml": "^4.1",
    "nikic/fast-route": "^1.3",
    "symfony/console": "^4.1"
  },
  "require-dev": {
    "phpunit/phpunit": "^6.5",
    "squizlabs/php_codesniffer": "^3.3",
    "jakub-onderka/php-parallel-lint": "^1.0"
  },
  "scripts": {
    "test": [
      "phpcs -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src",
      "parallel-lint . --exclude vendor",
      "phpunit -c ./phpunit.xml"
    ],
    "lint": [
      "phpcs -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src",
      "parallel-lint . --exclude vendor"
    ],
    "phpunit": "phpunit -c ./phpunit.xml",
    "fix-style": "phpcbf -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src"
  }
}
