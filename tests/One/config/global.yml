---
#
# 全局配置
#

# 服务名称
# name: one
# 服务版本
# version: 0.1
# 更新版本
stripeVersion: 2018-07-04
# PHP 安装路径
phpBin: /usr/local/bin/php
# 运行时目录
# runtimePath: '{ROOT_PATH}/runtime'
# 运行服务命令脚本路径
runCommand: '{ROOT_PATH}/one'
...